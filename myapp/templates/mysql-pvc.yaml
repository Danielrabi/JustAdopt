apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "myapp.fullname" . }}-mysql-pvc
spec:
  accessModes:
    - {{ .Values.mysql.persistentVolumeClaim.accessModes | default "ReadWriteOnce" }}
  resources:
    requests:
      storage: {{ .Values.mysql.persistentVolumeClaim.storage | default "1Gi" }}

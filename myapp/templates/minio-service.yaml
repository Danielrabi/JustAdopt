apiVersion: v1
kind: Service
metadata:
  name: {{ include "myapp.fullname" . }}-minio-service
spec:
  selector:
    app: {{ include "myapp.fullname" . }}-minio
  ports:
    - port: {{ .Values.minio.service.port }}
      targetPort: {{ .Values.minio.service.port }}
      name: api
  type: ClusterIP
apiVersion: v1
kind: Service
metadata:
  name: {{ include "myapp.fullname" . }}-mysql-service
spec:
  selector:
    app: {{ include "myapp.fullname" . }}-mysql
  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: {{ .Values.mysql.service.port }}